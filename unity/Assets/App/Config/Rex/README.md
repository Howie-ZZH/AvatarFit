# Rex Runtime Config

Unity config assets for Rex should live here:

```text
RexAnimationMapConfig.asset
RexOutfitMapConfig.asset
```

Use `FitGame -> Prepare Rex 3D Asset Folders` in Unity to generate placeholder assets.
